<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scubadive2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".JoinActivity"></activity>
        <activity android:name=".SendActivity" />
        <activity android:name=".AddAccountActivity" />
        <activity android:name=".AddLogActivity" />
        <activity android:name=".ModifyLogActivity"/>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD90vLXmdd9AYdBLbaQF0uYoyfBxTiYzmg" />

        <activity android:name=".MapActivity" />
        <activity android:name=".GetDataActivity" android:theme="@style/transparent"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>